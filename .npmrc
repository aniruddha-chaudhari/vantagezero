# react-simple-maps@3 (components/region-supply-map.tsx) declares a peer range that stops at
# React 18, but works fine on React 19. npm's default strict peer resolution fails the install
# outright on a clean checkout (Vercel runs `npm install`, not `npm ci`), so allow the legacy
# resolution the committed lockfile was already built with.
legacy-peer-deps=true
